services:
  survey-viewer:
    build: .
    ports:
      - "8765:8000"
    volumes:
      - ./survey_photos.yaml:/work/survey_photos.yaml
      - ./photos:/work/photos:ro
      - ./track:/work/track:ro
